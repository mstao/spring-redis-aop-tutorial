<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="com.han.dao.UserDao">

<!-- 用户信息 resultMap -->
 <resultMap type="com.han.entity.User" id="userResultMap">

   <id column="id" property="id"/>
   <result column="username" property="userName"/>
</resultMap>

<!-- 查询sql -->
 <sql id="query_user_where">
      
     <if test="userName != null and userName !=''">
     and username=#{userName} 
     </if>

    <if test="ids !=null and ids!='' ">
        <!-- foreach  遍历集合  拼接sql  id in(1,2) -->
         <foreach collection="ids" item="item" open="id in(" close=")" separator="," >
             #{item}
         </foreach> 
    </if>

 </sql>
 
<!-- 查询全部用户 -->
<select id="selectAll" resultMap="userResultMap">
   select * from user
</select>

<!-- 根据id获取用户信息 -->
<select id="selectByPrimaryKey"  parameterType="java.lang.Integer" resultMap="userResultMap">
      select * from user where id=#{id}
</select>


<!-- 查询所有用户-->
<select id="selectUserList" resultMap="userResultMap">
    select * from user
</select>


<!-- 添加用户 -->
<insert id="insert" parameterType="com.han.entity.User">
    <!-- 主键值返回 -->
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
        SELECT LAST_INSERT_ID()
    </selectKey>
    
    insert into user (username) 
    values(#{userName})
    
</insert>

<!-- 删除用户 -批量-->
<delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
   delete from user where id = #{id}
</delete>

<!-- 修改用户 -->
<update id="update" parameterType="com.han.entity.User">
    update user 
    <set>
          <if test="userName !=null and userName !=''">
            username=#{userName}
          </if>
    </set>
    where id=#{id}
</update>

</mapper>
